import React from "react";

const UserContext = React.createContext()

export default UserContext;

// Context provides PROVIDER. It will be a wrapper, and every children will have the access of it's contents[STATES].